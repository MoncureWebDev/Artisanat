
<div class="intro">

<div class="jumbotron artisanat-tron">
  <p>Welcome to</p>
  <h1>Artisanat</h1>
  <p>a marketplace for buying and selling home made crafts.</p>
</div>

<a href="#listings" class="btn btn-primary btnforlistings page-scroll">
  <!-- <i class="fa fa-angle-double-down animated"></i> -->
  <p>Check it out!</p>
</a>

</div>






<p id="notice"><%= notice %></p>

<!-- <h1>Listing Listings</h1>

<table class="listingsTable">
  <thead>
    <tr>
      <th>Image</th>
      <th>Name</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead> -->

<!--   <tbody>
    <%# @listings.each do |listing| %>
      <tr>
        <td><%#= image_tag listing.image.url(:thumb) %></td>
        <td><%#= listing.name %></td>
        <td><%#= listing.description %></td>
        <td><%#= listing.price %></td>
        <td><%#= link_to 'Show', listing, class: "btn btn-link" %></td>
        <td><%#= link_to 'Edit', edit_listing_path(listing), class: "btn btn-link" %></td>
        <td><%#= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <%# end %>
  </tbody>
</table> -->



<div class="center">

<div class="row" id="listings">
  <% @listings.each do |listing| %> <!-- Start of our Loop! -->
      <div class="col-md-3">
        <div class="thumbnail">
          <%#= image_tag listing.image.url(:thumb) %>
          <%= image_tag listing.image.url %> <!-- Changed this to make sure image fits Bootstrap "thumbnail" code -->
          <div class="caption">
            <h4><%= listing.name %></h4>
            <!-- <p><%#= listing.price %></p> -->
            <p class="pricetag"><%= number_to_currency(listing.price) %></p>


            <%= link_to 'Show', listing, class: "btn btn-link" %>
            <%= link_to 'Edit', edit_listing_path(listing), class: "btn btn-link" %>
            <%= link_to 'Destroy', listing, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %>
          </div>
        </div>
      </div>     
    <% end %>
</div>

        <%#= listing.description %>


</div>







<br>

<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
